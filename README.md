# Noam Zeise Portfolio Website

A static site generated by jekyll to showcase my projects.

### Notes

When adding an .scss file to `/_sass/` 
you also need to import it into `/assets/css/styles.scss`
